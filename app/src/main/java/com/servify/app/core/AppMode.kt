package com.servify.app.core

/**
 * Represents the active user mode.
 * CUSTOMER = Visual light mode, cinematic animations.
 * VENDOR = Dense dark mode, pro-tool behaviour, motion reduced.
 */
enum class AppMode {
    CUSTOMER,
    VENDOR
}
